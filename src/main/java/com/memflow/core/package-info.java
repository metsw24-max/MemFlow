/**
 * Core package containing direct off-heap direct memory management, custom character buffers,
 * zero-copy packet parsers, log indexing engines, and resource pooling allocators.
 */
package com.memflow.core;
